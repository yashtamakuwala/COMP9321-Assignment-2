class Recorder:
    def __init__(self, user_id, others, used_at, status_code):
        pass

    def record(self):
        session = Session()
        new_usage = USage(, last_name, email, hashed_password)
        session.add(new_user)
        session.commit()